name: fido2_pwa
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  web: ^0.5.1
  passkeys: ^2.15.1
  
flutter:
  uses-material-design: true
