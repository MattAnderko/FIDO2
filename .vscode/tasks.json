{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "docker: up backend stack",
      "type": "shell",
      "command": "docker compose up -d db cache backend gateway",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
